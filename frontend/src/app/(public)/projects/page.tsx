import React from 'react';

import PublicLayoutProvider from '@/providers/PublicLayoutProvider';

const Projects = () => {
  return <PublicLayoutProvider>Projects</PublicLayoutProvider>;
};

export default Projects;
